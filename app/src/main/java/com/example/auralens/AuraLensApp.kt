package com.example.auralens

import android.app.Application

class AuraLensApp : Application() {
    override fun onCreate() {
        super.onCreate()
        // Initialize things here if needed (e.g. logging)
    }
}
